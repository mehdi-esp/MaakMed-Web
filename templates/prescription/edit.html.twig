{% extends 'base.html.twig' %}

{% block title %}Edit Invoice{% endblock %}

{% block body %}
    <main class="container px-4 mx-auto">
        <div class="prose"><h2>Edit Prescription</h2></div>

        <twig:Prescription:Form form="{{ form }}" initialFormData="{{ prescription }}" button_label="Update"/>

        <a class="link link-secondary" href="{{ path('app_prescription_index') }}">back to list</a>

        {{ include('prescription/_delete_form.html.twig') }}
    </main>
{% endblock %}