{% extends 'base.html.twig' %}

{% block title %}Edit Invoice{% endblock %}

{% block body %}
    <h1>Edit Invoice</h1>


    <twig:InvoiceForm form="{{ form }}" initialFormData="{{ invoice }}" button_label="Update"/>

    <a href="{{ path('app_invoice_index') }}">back to list</a>

    {{ include('invoice/_delete_form.html.twig') }}
{% endblock %}
