{% extends 'base.html.twig' %}

{% block title %}Edit Invoice{% endblock %}

{% block body %}
  <main class="container px-4 mx-auto">
    <div class="prose"><h2>Edit Invoice</h2></div>

    <twig:Invoice:Form form="{{ form }}" initialFormData="{{ invoice }}" button_label="Update"/>

    <a class="link link-secondary" href="{{ path('app_invoice_index') }}">back to list</a>

    {{ include('invoice/_delete_form.html.twig') }}
  </main>
{% endblock %}
